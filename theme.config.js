/** @type {const} */
const themeColors = {
  // 暖橘色 — 強調色（按鈕、圖示、點綴）
  primary: { light: '#E8A87C', dark: '#E8A87C' },
  // 淺灰 — 主背景
  background: { light: '#EFEFEF', dark: '#1E1E1E' },
  // 純白 — 卡片底色
  surface: { light: '#FFFFFF', dark: '#2A2A2A' },
  // 深灰 — 主文字
  foreground: { light: '#333333', dark: '#F0EBE0' },
  // 中灰 — 次要文字
  muted: { light: '#888888', dark: '#8A8078' },
  // 淺灰邊框
  border: { light: '#E0E0E0', dark: '#3A3A3A' },
  // 成功
  success: { light: '#22C55E', dark: '#4ADE80' },
  // 警告
  warning: { light: '#F59E0B', dark: '#FBBF24' },
  // 錯誤
  error: { light: '#EF4444', dark: '#F87171' },
};

module.exports = { themeColors };
